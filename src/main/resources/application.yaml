server:
  port: 8081
  servlet:
    context-path: /api

spring:
  config:
    import: optional:classpath:application-secret.yaml

  datasource:
    url: "jdbc:mysql://swp391-server.mysql.database.azure.com:3306/club_management?ssl-mode=REQUIRED"

#      url: "jdbc:mysql://localhost:3306/club_management"
#      username: root
#      password: root
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: clubhubfpt@gmail.com
    password: "kwku usrt eeld uhju"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

app:
  base-url: "https://clubmanage.azurewebsites.net/api"
#  base-url: "http://localhost:8081/api"

payos:
  api-url: "https://api-merchant.payos.vn"

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    disable-swagger-default-url: true
  api-docs:
    path: /v3/api-docs
